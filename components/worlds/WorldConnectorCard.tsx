import type { WorldConnector } from '@/src/worlds/types';
import { Card } from '@/components/UI';
export function WorldConnectorCard({ connector }: { connector: WorldConnector }) { return <Card><p className="text-xs uppercase tracking-[0.25em] text-tide/70">{connector.worldType}</p><h3 className="mt-2 text-2xl font-semibold">{connector.name}</h3><p className="mt-2 text-white/60">source: {connector.source} · status: {connector.status}</p><p className="mt-3 text-sm text-plankton">权限：{connector.permissions.join(' / ')}</p><ul className="mt-3 space-y-1 text-sm text-white/55"><li>官方/授权：{['official_api', 'authorized_sdk'].includes(connector.source) || connector.licensePolicy.licenseType === 'authorized' ? '是' : 'mock/原创'}</li><li>只读/回放优先：{connector.permissions.includes('read_only') ? '是' : '否'}</li><li>禁止 live multiplayer control：{connector.capabilities.multiplayerLiveControl === false ? '是' : '否'}</li><li>反作弊边界：不外挂、不代练、不绕过反作弊</li></ul></Card>; }
