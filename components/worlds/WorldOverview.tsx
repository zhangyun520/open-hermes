import { Metric } from '@/components/UI';
import { listExperienceFeedback, listExperienceSessions } from '@/src/worlds';
import { mockWorldCapsules, mockWorldConnectors, sampleExperienceRewards } from '@/src/worlds/mockData';
export function WorldOverview() { const feedback = listExperienceFeedback(); const sessions = listExperienceSessions(); return <div className="grid gap-4 md:grid-cols-4"><Metric label="已连接世界" value={mockWorldConnectors.length} /><Metric label="世界胶囊" value={mockWorldCapsules.length} /><Metric label="今日体验" value={sessions.length} /><Metric label="接受反馈" value={feedback.filter((item) => item.status === 'accepted').length} /><Metric label="待审核动作" value={mockWorldCapsules.filter((item) => item.riskLevel === 'high' || item.status === 'reviewing').length} /><Metric label="高风险警告" value={mockWorldCapsules.filter((item) => item.riskLevel === 'high' || item.licensePolicy.licenseType === 'unknown').length} /><Metric label="体验奖励" value={sampleExperienceRewards.reduce((sum, reward) => sum + reward.amount, 0)} /><Metric label="禁止外挂" value="100%" /></div>; }
