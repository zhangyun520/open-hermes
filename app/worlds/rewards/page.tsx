import { Card, PageTitle } from '@/components/UI';
import { sampleExperienceRewards, sampleTesterReputation } from '@/src/worlds/mockData';
export default function WorldRewardsPage() { return <div><PageTitle eyebrow="Experience Rewards" title="体验者奖励">奖励不是按在线时长，而是按真实反馈质量、证据强度、残差价值和验证通过情况发放；不做可交易 token。</PageTitle><section className="grid gap-4 md:grid-cols-2">{sampleExperienceRewards.map((reward) => <Card key={reward.id}><p className="text-tide">{reward.rewardType} · {reward.status}</p><h2 className="mt-2 text-3xl font-semibold text-plankton">+{reward.amount}</h2><p className="mt-2 text-white/60">{reward.reason}</p></Card>)}<Card><h2 className="text-2xl font-semibold">Tester Reputation</h2><p className="mt-2 text-white/60">{sampleTesterReputation.trustLevel} · accuracy {sampleTesterReputation.feedbackAccuracy}% · accepted {sampleTesterReputation.acceptedFeedbackCount}</p></Card></section></div>; }
