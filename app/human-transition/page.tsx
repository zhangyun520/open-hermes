import { Card, PageTitle } from '@/components/UI';
import { transitionCards } from '@/lib/mock';
export default function HumanTransition() { return <div><PageTitle eyebrow="Human Transition Layer" title="人类安置层">不能用降低 D_task 的方式，制造更大的 D_human。</PageTitle><section className="grid gap-4">{transitionCards.map(t => <Card key={t.id}><p className="text-tide">{t.role} · 风险 {t.displacementRisk}</p><h3 className="text-2xl font-semibold">安置卡</h3><div className="mt-4 grid gap-4 md:grid-cols-2"><p>自动化任务：{t.tasksAutomated.join(' / ')}</p><p>人类保留任务：{t.tasksRemainingHuman.join(' / ')}</p><p>转型路径：{t.transitionPaths.join(' / ')}</p><p>训练需要：{t.trainingNeeded.join(' / ')}</p><p>收入保护：{t.incomeProtectionSuggestion}</p><p>贡献权：{t.contributionRights}</p></div><p className="mt-4 text-coral">尊严风险：{t.dignityRisk} · 审批：{t.approvalRequired ? '需要' : '不需要'}</p></Card>)}</section></div>; }
